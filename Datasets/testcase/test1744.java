package test;
class Source<T> {int value;
class InnerSource {private int calculate(Target<Integer> target, int x) {label: {int sum = 0;Source<String> src = new Source<>();SuperClass sup = new SuperClass(5);for (int i = 0; i < x; i++) {sum += target.data;if (sum > 100) break label;}Runnable r = () -> System.out.println(this.value);r.run();try {Class<?> cls = Class.forName("test.Target");cls.getMethod("toString").invoke(target);} catch (Exception e) {}return sum;}}
private int calculate(String str) {return str.length();}}
static class StaticNested {String name;}}
protected class Target {
U data;
class InnerTarget {}}
class SuperClass {SuperClass(int num) {}}
