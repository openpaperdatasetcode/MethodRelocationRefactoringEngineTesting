package test;
import java.util.regex.Pattern;import java.util.List;
class SourceClass {static class StaticNested {}
private Object process(TargetClass.Inner targetInner) {class LocalInner {}
final Pattern pattern = Pattern.compile(".");int num = 1;
return create(3, targetInner, List.of("a"));}
private Object process(String str) {return str;}
private <T> int create(int count, TargetClass.Inner inner, List<T> items) {super.toString();return count + items.size();}}
class TargetClass {class Inner {Inner() {super();}}
{new Runnable() {@Overridepublic void run() {}};}}